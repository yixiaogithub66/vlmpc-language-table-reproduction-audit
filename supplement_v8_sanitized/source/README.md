# Source Snapshot

This is the minimal source snapshot needed to audit the reported control branches and experiment launchers. It intentionally excludes `.env`, virtual environments, local logs, private gateway settings, and model weights.

The full project remains in the local workspace. The snapshot is for reviewer-facing traceability, not a turnkey installation of the simulator and its third-party assets.

